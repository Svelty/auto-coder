# Auto-Coder CLI

## Usage

### Install dependencies

```bash
yarn install
```

### Build the CLI

```bash
yarn build
```

### Link the CLI globally

```bash
yarn link
```

### Run the CLI

After linking, you can use the CLI anywhere on your system:

```bash
auto-coder [options]
```
